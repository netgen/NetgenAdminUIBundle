{% macro user_avatar( content ) %}{% spaceless %}
    {% include 'design:page_aside_user.tpl' %}
{% endspaceless %}{% endmacro %}

{% import _self as macros %}

{% set user_content = app.user.APIUser.content %}

<div class="aside-footer">
    {% set user_avatar = macros.user_avatar( user_content ) %}
    {% if user_avatar|trim is empty %}
        {% set user_avatar = asset( 'bundles/netgenmoreadminui/images/a0.jpg' ) %}
    {% endif %}

    <a href="#" class="user-dropdown-toggle dropdown-toggle" data-toggle="dropdown">
        <span class="user-avatar" style="background-image:url({{ user_avatar }})"></span>
    </a>

    <!-- dropdown -->
    <ul class="dropdown-menu user-dropdown">

        <li>
            <div class="user-avatar" style="background-image:url({{ user_avatar }})">
            </div>
            <h4 class="user-name">{{ ez_content_name( user_content ) }}</h4>
            <p class="user-role">{{ app.user.APIUser.login }}</p>
        </li>

        <li class="divider"></li>

        {% if ui_context != 'edit' and ui_context != 'browse' %}
            {% if ngmore_can_user( 'content', 'read', user_content ) %}
                <li><a href="{{ path( 'ez_urlalias', { 'contentId': user_content.id } ) }}">{{ 'ngmore_admin_ui.user.view_profile'|trans }}</a></li>
            {% endif %}

            {% if ngmore_can_user( 'content', 'edit', user_content ) %}
                <li>
                    <a href="{{ path( 'ez_legacy', { module_uri: '/content/edit/' ~ user_content.id } ) }}">{{ 'ngmore_admin_ui.user.change_information'|trans }}</a>
                </li>
            {% endif %}

            <li>
                <a href="{{ path( 'ez_legacy', { module_uri: '/user/password' } ) }}">{{ 'ngmore_admin_ui.user.change_password'|trans }}</a>
            </li>
        {% else %}
            <li><span class="disabled">{{ 'ngmore_admin_ui.user.view_profile'|trans }}</span></li>
            <li><span class="disabled">{{ 'ngmore_admin_ui.user.change_information'|trans }}</span></li>
            <li><span class="disabled">{{ 'ngmore_admin_ui.user.change_password'|trans }}</span></li>
        {% endif %}

        <li class="divider"></li>

        {% if ui_context != 'edit' or ui_context != 'browse' %}
            <li><span>Edit interface settings</span></li>
        {% endif %}
        {% if ui_context != 'edit' %}
            <li><span class="tt">Show Locations</span>
                <span class="control-toggle">
                    {% if ezpreference( 'admin_edit_show_locations' ) %}
                        <strong>on</strong><a href="{{ path( 'ez_legacy', { module_uri: '/user/preferences/set/admin_edit_show_locations/0' } ) }}" title="Disable location window when editing content.">off</a>
                    {% else %}
                        <a href="{{ path( 'ez_legacy', { module_uri: '/user/preferences/set/admin_edit_show_locations/1' } ) }}" title="Enable location window when editing content.">on</a><strong>off</strong>
                    {% endif %}
                </span>
            </li>
        {% endif %}

        {% if ui_context != 'browse' %}
            <li><span class="tt">Enable Re-edit</span>
                <span class="control-toggle">
                    {% if ezpreference( 'admin_edit_show_re_edit' ) %}
                        <strong>on</strong><a href="{{ path( 'ez_legacy', { module_uri: '/user/preferences/set/admin_edit_show_re_edit/0' } ) }}" title="Disable &quot;Back to edit&quot; checkbox when editing content.">off</a>
                    {% else %}
                        <a href="{{ path( 'ez_legacy', { module_uri: '/user/preferences/set/admin_edit_show_re_edit/1' } ) }}" title="Enable &quot;Back to edit&quot; checkbox when editing content.">on</a><strong>off</strong>
                    {% endif %}
                </span>
            </li>
        {% endif %}
        {% if ui_context != 'edit' or ui_context != 'browse' %}
            <li class="divider"></li>
        {% endif %}

        <li>
            {% if ui_context_edit %}
                <span class="disabled">{{ 'ngmore_admin_ui.layout.logout'|trans }}</span>
            {% else %}
                <a href="{{ path( 'logout' ) }}">{{ 'ngmore_admin_ui.layout.logout'|trans }}</a>
            {% endif %}
        </li>

    </ul>
    <!-- / dropdown -->

</div>
