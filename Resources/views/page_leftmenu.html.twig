{% set left_menu %}{% spaceless %}
    {% if current_menu_plugin != false %}
        {% include menu_plugins[current_menu_plugin].leftTemplate ignore missing %}
    {% endif %}
{% endspaceless %}{% endset %}

{% if left_menu|striptags|trim is not empty %}
    <div id="left-column" class="left-column layout-column">
        <div class="layout-column-inner">
            <div class="inner-cell">
                {{ left_menu|raw }}
            </div>
        </div>
    </div>
{% endif %}
